La Figure 1 montre cinq masques génériques de 8 bits.  L'Exemple 1 est 00000000, qui correspond à tous les bits de l'adresse dans l'octet.  L’Exemple 2 est 00111111, qui correspond aux deux premiers bits et ignore les six derniers bits de l'adresse.  L’Exemple 3 est 00001111, qui correspond aux quatre premiers bits et ignore les quatre derniers bits.  L’Exemple 4 est 11111100, qui ignore les six premiers bits de l’adresse et correspond aux deux derniers.  L'Exemple 5 est 11111111, qui ignore tous les bits de l'octet. La Figure 2 montre le résultat de l’application du masque générique 0.0.255.255 à une adresse IPv4 32 bits.  Le masque générique est considéré comme 16 bits zéro suivis de 16 bits un.  Le résultat correspond aux deux premiers octets dans une adresse et ignore les deux derniers octets.