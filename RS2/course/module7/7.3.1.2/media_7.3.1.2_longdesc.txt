La Figure 1 montre une entrée de liste d'accès en conflit sur une liste de contrôle d'accès en cours d'utilisation.  La première ligne est « access-list 3 deny 192.168.0.0 0.0.0.255 ».  Elle est suivie par la commande « access-list 3 permit host 192.168.10.10 », qui est en conflit avec l'instruction deny et déclenche l'envoi d'un message d'erreur à la console. La Figure 2 montre les deux mêmes commandes utilisées dans la figure précédente, mais dans l'ordre inverse. Cette séquence de commandes est acceptée par le routeur. La Figure 3 montre deux entrées qui ne sont pas en conflit.  La première commande est « access-list 5 deny 192.168.0.0 0.0.0.255 ».  Cette commande est suivie par « access-list 5 permit host 192.168.10.10 ».  L'hôte n'est pas touché par le réseau dans l'instruction deny, et il est donc accepté.