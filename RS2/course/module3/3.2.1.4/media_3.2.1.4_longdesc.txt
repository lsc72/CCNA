La Figure 1 affiche la sortie de la commande « show ip protocols » et met en surbrillance la section qui affirme que le routeur envoie les mises à jour de la version 1, mais peut recevoir celles des versions 1 et 2. La Figure 2 affiche les commandes permettant d’activer RIPv2 sur un routeur.  La figure commence en mode de configuration globale, puis active le protocole RIP avec la commande « router rip ».  La commande suivante est « version 2 » qui permet d’activer RIPv2 et de désactiver RIPv1.  Le résultat de la commande « show ip protocols » est affiché et maintenant, le routeur envoie et reçoit uniquement les mises à jour RIPv2. La Figure 3 affiche la sortie de la commande « show ip route pipe begin Gateway ». Cela permet d’afficher la table de routage dans laquelle il n’y a pas de routes RIP puisque dans la figure précédente la commande « version 2 » a permis d’activer uniquement RIPv2 et les mises à jour de RIPv1 sont désormais ignorées. La Figure 4 est un élément multimédia interactif qui permet à l’apprenant de configurer RIPv2 sur un routeur.