La figure illustre le processus de la vérification de la plus longue correspondance dans la table de routage.   L’adresse de destination pour le paquet est 172.16.0.10 et la première étape consiste à indiquer l’adresse au format binaire, qui est 10101100.00010000.00000000.00000000.00001010.  Il existe trois routes qui peuvent éventuellement être utilisées pour atteindre la destination, alors le routeur doit vérifier chaque route et comparer les valeurs binaires de gauche à droite. Celle qui correspond au plus de bits dans le masque de sous-réseau sera la route choisie.  Les trois choix sont un /12, un /18 et un /26.  Le paquet de destination correspond au plus grand nombre de bits avec le /26, ce sera donc la route sélectionnée.